groups:
  - name: topology
    rules:
      - alert: TopologyProjectionStale
        expr: topology_projection_lag_seconds > 5
        labels: { severity: warn }
        annotations:
          summary: "Topology Neo4j projection lag"
          runbook: "https://docs.synanton.dev/runbooks/topology-projection"
