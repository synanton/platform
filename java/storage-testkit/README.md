# storage-testkit

Adapter-agnostic contract suites (test-fixtures): `SynvaultStoreContract`,
`SynquestEngineContract` (behavior), `ConformanceGatingContract` (flags vs
matrix vs loadable evidence). Concrete adapters subclass per port side; a
passing suite run IS the conformance evidence named in matrices. Phase-0 PoC scope.
