# synvault-inmemory

Fully conforming in-memory `SynvaultStore` (transactions, OCC, cursor
pagination, mandatory provenance) for tests and lightweight development.
Records metrics by default. Serves as the reference implementation the gating
suite compares others against. Phase-0 PoC scope.
