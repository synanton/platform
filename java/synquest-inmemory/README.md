# synquest-inmemory

Fully conforming in-memory retrieval + projection writer/admin (lexical/vector/
hybrid, pre-ranking eligibility, ordering-key regression guard,
generation-scoped delete). Brute-force scoring with semantic-tolerance
comparison (never byte parity). Metrics default-on. Phase-0 PoC scope.
